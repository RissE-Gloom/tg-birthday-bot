name: Keep Render Alive
on:
  schedule:
    - cron: '*/5 * * * *'  # Каждые 5 минут
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
    - name: Ping Render
      run: |
        curl -s "https://tg-birthday-bot.onrender.com/health"
